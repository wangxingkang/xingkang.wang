# 将二维数组转换为 HTML 表格字符串

import { Badge } from '@rspress/core/theme';

<Badge text="中等" type="warning" />
<Badge text="JavaScript" type="tip" />
<Badge text="手写代码" type="tip" />

## 题目描述

将只包含数字的二维数组，转换为 HTML 表格字符串，要求将每列的最大值高亮;

```ts
function arrayToHighlightedTable(arr: number[][]): string {

}

// 验证
arrayToHighlightedTable([
  [6, 5, 1, 2],
  [1, 2, 1, 1],
  [3, 5, 0, 1],
]);
```
